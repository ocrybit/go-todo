# A Simple Todo App in Go

## Run

```bash
go run .
```

## Commands

- `s | show` : show tasks
- `a | add` : add a task
- `c | complete` : complete a task
- `d | del` : delete a task
- `t | trash` : trash completed tasks
- `q | quit` : quit the app
